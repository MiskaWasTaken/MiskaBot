export const token = ''
export const google = '' (google and ytapikey are the same thing, google how to get a yt api key)
export const ytapikey = '' 
export const client_id = '' (follow these steps: https://github.com/googlesamples/assistant-sdk-nodejs)
export const client_secret= ''
export const refresh_token = ''
export const hypixel_key = '' (get on mc.hypixel.net and do /api new get the string of numbers and letters and put that here

export default {
    token,
    ytapikey,
    client_id,
    client_secret,
    refresh_token,
    hypixel_key
}
